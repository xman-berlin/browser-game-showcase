// Shadow map type is configured via the Canvas `shadows` prop in App.tsx.
// This component is a no-op placeholder — shadow enable/disable is handled
// by the Canvas gl prop and directional light castShadow toggles.
export default function ShadowSetup() {
  return null
}
